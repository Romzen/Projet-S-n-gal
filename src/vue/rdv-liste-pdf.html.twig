<style>
    table {
        border: 1px solid black;
        border-collapse: collapse;
        margin-top: 15px;
        margin: auto;
        margin-right: auto;
    }
    th {
        background-color: #3498DB; 
        color: #FFF;
        padding: 5px;
    }
    tr, td, th {
        text-align: center;
        padding-left: 10px;
        padding-right: 10px;
        border: 1px solid black;

    }

    td {
        padding: 10px;
    }
    
    h1 {
        text-align: center;
          font-family: Helvetica, Arial, Sans-Serif;
    }

</style>



<u><h1>Liste des rendez-vous :</h1></u>


    <table>
        <thead>
            <tr>
                <th>Patient</th>
                <th>Date du RDV</th>
                <th>Profil sérologique</th>
                <th>Sexe</th>
                <th>Dernière dispensation</th>
            </tr>
        </thead>    
            
            {% for lp in listeP%}
                {%for lr in listeRDV%}
                    {% if lp.id_patient == lr.id_patient%}

                        <tr>
                            <td>{{lp.num_id_national}}</td><td>{{lr.rdv}}</td><td>{{lp.nom_profil}}</td><td>{{lp.sexe}}</td><td>{{lp.maxDispen}}</td>
                        </tr>

                    {%endif%}
                {%endfor%}
            {%endfor%}
</table>
